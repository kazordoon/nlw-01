/* eslint-disable semi */
export default interface PointData {
  point: {
    name: string;
    image: string;
    email: string;
    whatsapp: string;
    uf: string;
    city: string;
  };
  items: {
    title: string;
  }[];
}
